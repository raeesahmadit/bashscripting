Arithmetic Opertaions:
            x=5
            y=3
            let mult=$x*$y  (let will assume this is mathametical not string else it will only show the values)
            echo $mul
or     echo "subtraction is $(($x-$y))"
       echo "addition is:" $(($x+$y))

