#!/bin/bash

fs=$(free -mt | grep "Total" | awk '{print$4}')
th=500
if [[ $fs -le $th ]]
then 
   echo "Warnning!! Ram is getting low: $fs M"
else
   echo "You have sufficient Ram yet, almost: $fs M"
fi