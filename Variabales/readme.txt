to define a variable : name="raees"
                       age=30
                       echo "my name is $name and age is $age"
                       name="ahmad"
                       echo "my new name is $name"

to store a output of a linux command in a variable:  host=$(hostname)
                                                     echo "name of machine is $host"
constant variable : readonly bulitby="raees" 